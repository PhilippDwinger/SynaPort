# SynaPort
SynaPort is a communication and computing server that allows external applications to connect to and use neural networks through HTTP. It can create, train, and restore neural networks in memory without permanently storing them.
